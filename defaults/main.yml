---
zsh_dependencies: []

zsh_config_mode: 0644
zsh_config_backup: true
zsh_config_overwrite: false

zsh_users:
  - "{{ ansible_user }}"

zsh_users_config:
  - template: "zshrc.j2"
    filepath: "$HOME/.zshrc"

zsh_system_config: []
